fn main() {
    let numbers = vec![10, 20, 30, 40, 50];
    let slice = &numbers[1];
    println!("slide is {:?}", slice);
}

// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>
int main() {
    // Write C code here
    char *s = "🦀";
    printf("%zu\n bytes", strlen(s));

    return 0;
}
